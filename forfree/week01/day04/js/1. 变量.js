// id 获取元素对象
// classname 和 tagname 获取的是元素集合(类数组)


// es6 
var let = 0;



//形参和实参
/**
 * 形参和arguments
 * 1. 形参直接赋了实参的值
 * 2. 但 arguments 是一个堆内存空间，如果 arguments[0] 还有到堆内存空间找。影响性能
 * 3. 所以能用形参就直接用形参
 */

// 函数执行会保护里面私有变量不受外界的干扰，这种保护机制叫"闭包"(但不是个严格的闭包)。
// 函数中的函数，形成一个不会销毁，且有私有变量的函数



